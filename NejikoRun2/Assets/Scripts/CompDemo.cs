using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

[AddComponentMenu("Demo/Escape")]
[Serializable]
public class CompDemo : MonoBehaviour
{
    float speed;
    float speedx;
    float speedy;
    float speedz;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("でけた！！");
    }

    // Update is called once per frame
    void Update()
    {
    }
}
